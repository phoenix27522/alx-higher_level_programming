#!/usr/bin/python3
""" checking an object have the same instance """


def is_same_class(obj, a_class):
    """ function that check if an object has the same instance """
    return type(obj) is a_class
